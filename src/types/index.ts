/**
 * Shared project-level TypeScript types.
 * Add client-specific types here after forking.
 *
 * Examples:
 *   export interface TeamMember { name: string; role: string; image: string }
 *   export interface Service { title: string; description: string; icon: string }
 */
